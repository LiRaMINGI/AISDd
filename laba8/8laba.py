def radix_sort(arr):
    # Находим максимальное число, чтобы определить количество разрядов
    max_num = max(arr)
    # Сортируем каждый разряд с помощью подсчёта
    exp = 1
    while max_num // exp > 0:
        count_sort_radix(arr, exp)  # Сортируем по текущему разряду
        exp *= 10  # Переходим к следующему разряду
    return arr